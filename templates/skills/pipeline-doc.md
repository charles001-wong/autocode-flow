# {{project_name}} — autocode Pipeline

> Auto-generated by `autocode-flow`. Last updated: {{timestamp}}

## Overview

This document describes the automated development workflow configured for **{{project_name}}**.

## Tech Stack

| Component       | Choice                           |
|-----------------|----------------------------------|
| Language        | {{language}}                     |
| Framework       | {{framework}}                    |
| Package Manager | {{package_manager}}              |
| Test Framework  | {{test_framework}}               |
| Linter          | {{linter}}                       |
| CI/CD           | {{ci_cd}}                        |

## Pipeline Stages

### 1. Plan (`/plan`)
{{plan_description}}

### 2. TDD (`/tdd`)
{{tdd_description}}

### 3. Code (`/code`)
{{code_description}}

### 4. Test (`/test`)
{{test_description}}

{{#if e2e_enabled}}
### 5. E2E (`/e2e`)
{{e2e_description}}
{{/if}}

{{#if deploy_enabled}}
### 6. Deploy (`/deploy`)
{{deploy_description}}
{{/if}}

## Commands Quick Reference

| Command   | Description                                    |
|-----------|------------------------------------------------|
| `/plan`   | Analyze requirements and create implementation plan |
| `/tdd`    | Start TDD cycle (RED → GREEN → REFACTOR)       |
| `/code`   | Implement following coding standards            |
| `/test`   | Run tests and check coverage                   |
{{#if e2e_enabled}}| `/e2e`    | Run end-to-end tests                           |{{/if}}
{{#if deploy_enabled}}| `/deploy` | Verify deployment readiness                    |{{/if}}

## Configuration

The full configuration is stored in `.cursor/skills/autocode/config.json`.
To reconfigure the pipeline, run `/autocode-new` again.
